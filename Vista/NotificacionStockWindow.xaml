<Window x:Class="Project_v1.Vista.NotificacionStockWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⚠ Stock Bajo" Height="450" Width="680"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                   Text="⚠ Productos con Stock Bajo"
                   FontSize="20" FontWeight="Bold"
                   Foreground="#E67E22"
                   Margin="0,0,0,8"/>

        <!-- Subtítulo -->
        <TextBlock Grid.Row="1"
                   Text="Los siguientes productos tienen 10 unidades o menos en inventario."
                   FontSize="13" Foreground="#7F8C8D"
                   Margin="0,0,0,15"/>

        <!-- Tabla -->
        <DataGrid Grid.Row="2"
                  x:Name="TablaStock"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  CanUserResizeRows="False"
                  Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="45"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                <DataGridTextColumn Header="Marca" Binding="{Binding Marca}" Width="100"/>
                <DataGridTextColumn Header="Categoría" Binding="{Binding Categoria}" Width="100"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="60">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#E74C3C"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Total -->
        <TextBlock Grid.Row="3"
                   x:Name="TxtTotal"
                   FontSize="13" FontWeight="Bold"
                   Foreground="#E74C3C"
                   Margin="0,0,0,12"/>

        <!-- Botones -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="📊 Exportar a Excel"
                    Click="BtnExportarExcel_Click"
                    Padding="15,8" Margin="0,0,10,0"
                    Background="#1D6A39" Foreground="White"
                    FontSize="13" FontWeight="Medium"/>

            <Button Content="Entendido"
                    Click="BtnCerrar_Click"
                    Padding="20,8"
                    Background="#2C3E50" Foreground="White"
                    FontSize="13"/>
        </StackPanel>
    </Grid>
</Window>